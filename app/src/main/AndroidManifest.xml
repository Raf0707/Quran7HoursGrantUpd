<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Только Android 8/9 для legacy-кэша в Downloads -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Уведомление о скачивании аудио -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground WorkManager для длительного скачивания -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".QuranApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_quran"
        android:label="Коран за 7 часов"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quran7Hours"
        android:usesCleartextTraffic="false">

        <!--
            WorkManager использует этот foreground-service во время
            скачивания полного набора аудио чтеца.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>